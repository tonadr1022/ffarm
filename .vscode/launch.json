{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Build and Run",
      "type": "lldb",
      "request": "launch",
      "preLaunchTask": "build",
      //      "runtimeExecutable": "${workspaceFolder}/build/main",
      "program": "${workspaceFolder}/build/main",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}